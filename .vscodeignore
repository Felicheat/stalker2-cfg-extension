# Exclude node_modules (except for those needed at runtime)
node_modules/**

# Exclude source files (if not needed at runtime)
src/**

# Exclude configuration files
tsconfig.json
.eslintrc.json
.vscode/**
.gitignore
README.md
LICENSE
